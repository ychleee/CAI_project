# Core Dependencies
torch>=2.0.0
transformers>=4.35.0
datasets>=2.14.0
huggingface_hub>=0.17.0
accelerate>=0.24.0
peft>=0.6.0  # For LoRA/QLoRA
trl>=0.7.0  # For PPO training
bitsandbytes>=0.41.0  # For quantization
einops>=0.7.0

# NeMo Framework (optional - for full NeMo integration)
# nemo-toolkit[all]  # Uncomment if using full NeMo

# Model Training
wandb>=0.15.0  # Experiment tracking
tensorboard>=2.14.0
safetensors>=0.4.0

# Data Processing
numpy>=1.24.0
pandas>=2.0.0
jsonlines>=3.1.0
pyyaml>=6.0
tqdm>=4.65.0

# Evaluation
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
scipy>=1.11.0

# Utilities
python-dotenv>=1.0.0
rich>=13.5.0  # Better terminal output
typer>=0.9.0  # CLI interface
pytest>=7.4.0  # Testing
black>=23.0.0  # Code formatting
isort>=5.12.0  # Import sorting

# Colab-specific (install separately in notebooks)
# google-colab
# gdown  # For downloading from Google Drive